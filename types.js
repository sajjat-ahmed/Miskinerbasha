export const GenderPreference = {
  MALE: 'Male',
  FEMALE: 'Female',
  ANY: 'Any'
};

export const RoomType = {
  SINGLE: 'Single',
  SHARED: 'Shared'
};

// Note: Type definitions (Room, User, BookingRequest) were TypeScript-only and
// are not required at runtime. This file provides runtime enums used by mock data.
